import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.vocalsalvosporcristo.app",
  appName: "Vocal Salvos por Cristo",
  webDir: "dist",
};

export default config;